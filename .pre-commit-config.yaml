repos:
  - repo: local
    hooks:
      - id: check-urls
        name: Check URLs in cv.yaml
        entry: python3 check-urls.py cv.yaml
        language: system
        always_run: true
        pass_filenames: false

